interface Motor {
  maxGradibility: number;
  maxSpeed: number;
}

// C30
export const speedMotor: Motor = {
  maxGradibility: 30,
  maxSpeed: 80
};

// C38
export const torqueMotor: Motor = {
  maxGradibility: 35,
  maxSpeed: 68
};
