export enum DiameterUnits {
  cm = 'cm',
  in = 'in',
}

export enum WidthUnits {
  cm = 'cm',
  in = 'in',
}

export enum SpeedUnits {
  kmh = 'kmh',
  mih = 'mih'
}

export enum RangeUnits {
  km = 'km',
  mi = 'mi'
}

export enum WeightUnits {
  kg = 'kg',
  lb = 'lb',
}

export enum GroundClearanceUnits {
  mm = 'mm',
  in = 'in',
}

export enum DimensionsUnits {
  mm = 'mm',
  in = 'in',
}

export enum AvailableTheme {
  auto = 'auto',
  dark = 'dark',
  light = 'light',
}
